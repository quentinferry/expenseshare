import React from 'react';

function CreatePage(props) {
    return (
        <React.Fragment>
            <h1>Create an event</h1>
            <p>Create an event and sendthe henerated identifier to others!</p>
        </React.Fragment>

    );
}

export default CreatePage